# Local AI Workstation v1.0-beta.1

产品包: `local-ai-workstation-v1.0-beta.1_x64.zip`
发布日期: 2026-06-05
状态: Capability Package A / Controlled Knowledge Loop (候选包)

## 版本概念分离 (重要)

- **产品版本** = `Local AI Workstation v1.0-beta.1`
  - 仅出现在: 产品包名 / release note / `VERSION.md` / `CAPABILITY-A.md` / `ACCEPTANCE-P1-P2.md`
  - 不出现在: winwork 应用内任何 UI 字段
- **组件版本** = 各仓独立 semver
  - `winwork 0.2.33` (应用内 About / 顶栏 pill 显示此值)
  - `wind-cli 0.3.0` (通过 `windcli --version` 查询)
  - `llm-wiki-lib 0.1.5` (Cargo dep, 不直接对外)

## 3 仓库唯一基线

| 仓库 | 版本 | commit | 备注 |
| --- | --- | --- | --- |
| winwork | 0.2.33 | `8df54fca` | Batch 1 5 commit 栈 |
| wind-cli | 0.3.0 | `3336cd65` | full build (含 wiki 子命令) |
| llm-wiki-lib | 0.1.5 | `a156fa93` | Cargo dep, 由 wind-cli 引入 |

## 能力包范围 (Capability Package A)

- M5 知识库最小闭环 (`winwork → wind-cli → llm-wiki-lib` 间接路径)
- M3 对话主链路可控 (6 类意图 + 显式确认中间态)
- 实现 commit 栈: `489bdefc` → `4de64d49` → `843d050c` → `be116c1e` → `8df54fca`

## 真机验收入口

仅 `AC-P1` (文件 ingest → 知识查询) + `AC-P2` (对话意图分类 + 显式确认 + trace 可见, 含 P2.6 read-only 反例), 详见 `ACCEPTANCE-P1-P2.md`。

## 已知范围外残留

- `init_demo_workspace`: audit-invoke 报 1 个 invoke 缺 handler
- 不影响 AC-P1 / AC-P2, 不属于本候选包放行范围
- 留待后续 hotfix 或删除旧 invoke 调用
