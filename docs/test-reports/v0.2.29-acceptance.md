# v0.2.29 测试验收报告

日期：2026-06-04
版本：v0.2.29
报告人：@coder
对应规格：`docs/superpowers/specs/2026-06-04-winwork-v0.2.29-usable-release-design.md`
对应实现计划：`docs/superpowers/plans/2026-06-04-winwork-v0.2.29-implementation-plan.md`
状态：In Review
来源 commit：`6f79686911aecef3164bce7e8b3de551cbdd203c` (`origin/master`)
Release：`https://github.com/wbyanclaw/winwork/releases/tag/v0.2.29`

## 0. 报告使用说明

每行验收点都按 spec §7 的最小字段集记录：
- 用例 ID（AC-xxx，与 spec 章节一一对应）
- 工程侧验证（headless 截图 / sha256 / 自动化测试结果）
- 用户侧实机验证（Windows exe 由 @kevinywb / @yan 在真机运行）
- 结论（Pass / Fail / Partial + 备注）

**没有"用户侧实机"那一栏填值，不允许宣称"版本可用"。**（来源：主规格 4.4 / 7）
本报告的所有"用户侧"条目当前状态为 `Pending`，需要 Windows 真机回归。

工程侧验证 4 类 gate：
- `cargo check` — Rust 编译
- `scripts/check-inline-scripts.sh` — 每个 inline `<script>` 块 `node --check`
- `scripts/audit-invoke.sh` — 前端 `invoke(...)` vs 后端 `generate_handler!` 差集
- `npm test` — 14 个 runtime 单测

## 1. 整体结论

| 项 | 结论 |
| --- | --- |
| 验收点总数 | 14 |
| 工程侧 Pass | 14 |
| 工程侧 Fail / Partial | 0 |
| 用户侧实机 Pass | 0 (pending Windows run) |
| 用户侧实机 Pending | 14 |
| **整体版本可用** | **Pending (工程侧通过, 等用户实机回归)** |

按主规格 8.0 规则：v0.2.29 可在工程侧宣称"基础可用", 但需要 Windows 实机回归后填齐 14 个 AC 的"用户侧"栏, 才能判 done.

## 2. 验收点逐条

### 2.1 L0 验收点 (UI 骨架稳定)

#### AC-001 — 首屏 4 胶囊填实不卡占位 (spec §4.1 / 3.1 L0)
- **用例**：全新启动 + 标准 workspace, 观察 4 胶囊 (就绪 / 模型 / winwork ver / wind-cli ver) 实时填实
- **工程侧**：
  - L0 fix commit: `ca9a5144` 删 stale switchTab 重复声明 (整个 inline script 块解析失败 → 首屏卡死)
  - L0 fix 验证: `scripts/check-inline-scripts.sh` → 5/5 pass
  - 4 胶囊驱动函数 (`checkAllEnv`) 已存在, 后端 handler `get_environment_status` (A group 已在) + `check_llm_wiki` (A group 新增) 注册
  - 等待时间上限: spec §3.1 L0.1 要求"合理时间内填实"; 实现路径 4 个 env check 全部并行 (Promise.all), 单个 wind-cli 调用 < 1s 常态
- **用户侧**：
  - 测试人：@kevinywb (Pending)
  - 操作：解压 `winwork_0.2.29_x64.zip` → 双击 `winwork.exe`
  - 期望：4 胶囊在 1-2 秒内从 `…` 变为实值; 不再卡死
  - 结论：Pending
- **结论**：工程侧 Pass, 用户侧 Pending

#### AC-002 — 切对话 / 切文件 / 切 tab 不抛前端异常 (spec §3.1 L0 稳定可测定义 #2)
- **用例**：在多个对话 / 文件 / 3 个 detail tab 之间切换
- **工程侧**：
  - L0 fix 重点: stale `switchTab` 修复后, 切 detail tab 的核心函数定义唯一, 不再 throw
  - `scripts/check-inline-scripts.sh` → 5/5 pass (任何静态 parse error 都会被这个 gate 拦下)
  - `cargo check` → 0 warning
- **用户侧**：
  - 测试人：@kevinywb (Pending)
  - 操作：新建对话 → 切回旧对话 → 点击 SYSTEM.md 切 detail-file tab → 切 detail-trace tab → 切 detail-source tab
  - 期望：每次切换无 console error, 无白屏
  - 结论：Pending
- **结论**：工程侧 Pass, 用户侧 Pending

#### AC-003 — v0.2.27 模板字面量 / 空元素引用 regression 不再现 (spec §3.1 L0 稳定可测定义 #3)
- **用例**：`node --check` 所有 inline `<script>` 块 + 静态扫描
- **工程侧**：
  - 静态扫描: `scripts/check-inline-scripts.sh` 自动覆盖整个 src/index.html 的所有 inline script 块, 命中数 = 0
  - v0.2.27 类反引号反斜杠问题: 已收入 gate, 任何 inline script 块解析失败会让 release 不可发布
  - 空元素引用: v0.2.27 时代 `apiKeyStatus` / `apiKeyBtn` null deref 已修; 当前 `getElementById` 配合 v0.2.28 起的 element id 设计不再悬挂
- **用户侧**：
  - 测试人：@kevinywb (Pending)
  - 命令：触发任何 wiki_*/mkdir/wft/run_command (B/C 组降级路径)
  - 期望：不抛 SyntaxError / null deref
  - 结论：Pending
- **结论**：工程侧 Pass, 用户侧 Pending

### 2.2 L1 验收点 (wind-cli 依赖入口)

#### AC-101 — 首次启动可正确探测 wind-cli (spec §4.1 / 3.1 L1.1)
- **用例**：
  - 路径 A：用户机器已有 windcli / wind
  - 路径 B：用户机器没装 wind-cli
- **工程侧**：
  - 顶栏探测函数 `checkAllEnv` 存在, 通过 `get_environment_status` (lib.rs:124, 已在) + `check_llm_wiki` (lib.rs 新增, A group) 两条路径
  - 后端实现: `wind::check_windcli` (wind.rs:463) + `wind::check_llm_wiki` (wind.rs:607) 都已存在
  - L1 install modal 显示逻辑未改 (沿用 v0.2.28 已有)
- **用户侧**：
  - 测试人：@kevinywb (Pending)
  - 路径 A: 已装 wind-cli → 4 胶囊 wind-cli 栏显示具体版本
  - 路径 B: 未装 wind-cli → 弹 install modal, 按钮文案"⚡ 一键安装 wind-cli"
  - 结论：Pending
- **结论**：工程侧 Pass, 用户侧 Pending

#### AC-102 — 未安装时 install 入口可用, invoke 链路不断 (spec §3.1 L1.2 / 5.2)
- **用例**：在路径 B 状态下点"一键安装 wind-cli"按钮
- **工程侧**：
  - audit-invoke.sh 验证: `trigger_install` 已在 `lib.rs:300` 注册, audit 差集 = 0
  - 后端实现: `trigger_install` 调 `opener_open_url("https://github.com/wbyanclaw/wind-cli/releases/latest")`, 返回 `WindResult { ok: true, stdout: "Opened: <url>" }`
  - 前端实现: index.html:824 invoke('trigger_install') 不再 throw
  - fallback URL: 走 github.com releases/latest 固定路径, 避免被改
- **用户侧**：
  - 测试人：@kevinywb (Pending)
  - 操作: 全新 Windows 机器 (无 wind-cli) → 装 release zip → 启动 → 点安装按钮
  - 期望: 默认浏览器打开 wind-cli GitHub releases 页
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-103 — 安装/检查/升级结果回 UI 是结构化状态 (spec §3.1 L1.4)
- **用例**: 观察顶栏 4 胶囊的实时变化
- **工程侧**:
  - 后端返回 `serde_json::Value` 而不是字符串 (lib.rs:124 `get_environment_status` 返回 `{ winworkVersion, windcli, wiki }`)
  - 前端解析函数: `parseWindVersion` 已用正则, 不靠字符串包含判断
  - 4 胶囊更新链路: `getEnvironmentStatus()` → `updateTopBar()` → DOM 更新
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 操作: 装 wind-cli → 重启 winwork → 4 胶囊的 wind-cli 字段应从 `未就绪` 变为具体版本
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

### 2.3 L2 验收点 (工作区最低可用交互)

#### AC-201 — 文件树稳定列出 workspace + wiki (spec §3.1 L2.1)
- **用例**: 加载标准 workspace (SYSTEM.md / README.md / notes/ / deliverables/)
- **工程侧**:
  - 后端: `list_workspace` + `list_wiki` 已在 lib.rs 注册 (A group), 调 `wind::list_files(None)` 和 `wind::list_files(Some(get_wiki_path()))`
  - 前端: `loadFileTree` (index.html:490) 在 init 阶段调两个 list invoke, 并行 (Promise.all pattern, 5s timeout)
  - 排序: folder 在前, 同类按名字升序 (index.html:539)
  - SYSTEM.md 是产品级 first-class node, 第一项 + ⚙️ + 蓝色 SYSTEM 标签
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 截图: Pending
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-202 — 对话中可发起针对工作区的基本查询 (spec §3.1 L2.1)
- **用例**: 对话中输入"列出 workspace 下的所有 .md 文件"
- **工程侧**:
  - 主路径: `ai_chat` (lib.rs 新增, A group) → `reqwest` POST 到 `{baseUrl}/chat/completions` (OpenAI 兼容), 返回 `{ response, commands_executed: [], command_results: [] }`
  - 工具调用通过 orchestrator 路径: `runtime.ingestWiki` + `runtime.writeArtifact` + `runtime.runTool` (B group degrade 后走 fallback)
  - 关键词路由 (无 apiKey): `mkdir` / `wft` / `run_wind_command` (B group) try/catch 后返回 ok:false, 不崩
  - `wiki_query` / `wiki_ingest` / `wiki_lint` (C group) 不调 invoke, 显式 amber card
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 操作: 配置 apiKey → 提问 → 验证 AI 响应 → 关键词路由测试
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-203 — `.md` 文件 markdown 预览, 非纯文本 (spec §3.1 L2.2)
- **用例**: 点击 SYSTEM.md
- **工程侧**:
  - 修复: 新加 `<script src="js/lib/marked.min.js">` (index.html:10), marked v15.0.12 现在真的加载 (之前 vendored 但未加载, chat 渲染也是静默 fail)
  - showFileContent (index.html:631) 检测 `.md`/`.markdown` 后缀 → `marked.parse(content)` → 包 `markdown-body` div
  - 失败 try/catch fallback 到纯文本
  - sha256: index.html = `54c7d3942df7f8814f41c301f52800be54ee0e4285991f232a742e473c032c70`
  - marked.min.js sha256: `3e7e7d7feb3e5d58cb6c804f68ab5c24cc7e5eb6270fd6e5cbb9124739217d0c`
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 操作: 启动 → 等文件树加载 → 点击 SYSTEM.md → 切到"文件信息" tab
  - 期望: 看到 markdown 渲染 (标题 / 列表 / 代码块 / 强调有样式)
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-204 — 普通文本文件以可读文本显示 (spec §3.1 L2.2)
- **用例**: 点击一个 .txt 或无后缀文件
- **工程侧**:
  - showFileContent 在 `!isMarkdown` 分支保持原有 `<pre>` 渲染 (等宽字体 + 保留空白), 不走 marked
  - 测试用例: `.txt` / `.json` / 无后缀文件 都走文本分支
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 截图: Pending
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-205 — 文件名 / 路径分段模糊匹配搜索 (spec §3.1 L2.3)
- **用例**: 在左栏搜索框输入 "note", 期望 notes/ 2026-Q2.md 高亮
- **工程侧**:
  - 新加 `<input id="fileSearch">` (index.html:295)
  - `filterFileTree(query)` (index.html:618) — debounce 120ms, hide 不匹配的 `.tree-item` (folder header workspace/ + wiki/ 始终可见)
  - 反馈: `fileSearchCount` 显示 "N 项匹配" 或 "无匹配项"
  - 纯前端, 无后端 search_files handler (符合 spec 5.3 "预览 > 搜索 > 查询" 顺序)
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 操作: 启动 → 输入 "note" → 验证仅 notes/* 显示 → 清空 → 全部显示
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

#### AC-206 — 点击文件 → 打开详情 / 预览, trace 可见 (spec §3.1 L2.4)
- **用例**: 在左栏点 SYSTEM.md → 右侧详情滑出
- **工程侧**:
  - file-tree (index.html:534) `systemDiv.onclick = () => showFileContent('SYSTEM.md')`
  - showFileContent → read_file invoke → detail-file tab 更新
  - trace tab: 之前已有 `displayWindResult` 把 wind-cli 命令/参数/退出码/时长写入 detail-trace
- **用户侧**:
  - 测试人: @kevinywb (Pending)
  - 截图: Pending
  - 结论: Pending
- **结论**: 工程侧 Pass, 用户侧 Pending

### 2.4 工程交付材料

#### AC-301 — 开发规格书存在
- **工程侧**:
  - 路径: `docs/superpowers/specs/2026-06-04-winwork-v0.2.29-usable-release-design.md`
  - 状态: Formal (2026-06-04 by @yan, 收口为 formal baseline by @ruler)
  - commit: `7a67e188`
- **结论**: Pass

#### AC-302 — 本测试验收报告存在
- **工程侧**:
  - 路径: `docs/test-reports/v0.2.29-acceptance.md` (本文件)
  - commit: 在 v0.2.29 release 流程中
- **结论**: Pass

## 3. 已知未覆盖 (按主规格 3.2, v0.2.29 不承诺)

| 项 | 前端降级 | 后端实现 | 状态 |
| --- | --- | --- | --- |
| wiki ingest / query / lint | 关键词路由 + doWikiIngest/doWikiLint 全部走 amber card 提示 v0.3.0 | C 组, 不实现 | v0.3.0 (task #3) |
| llm-wiki-lib 独立能力 | 不在范围 | 不实现 | v0.3.0 |
| 一键入库可立即检索完整工作流 | 不在范围 | 不实现 | v0.3.0 |
| 跨组件知识库依赖协调 | 不在范围 | 不实现 | v0.3.0 |
| 工作区内容搜索 (grep) | 不在范围 | 不实现 | v0.3.0+ |
| 工作区 glob pattern 匹配 | 不在范围 | 不实现 | v0.3.0+ |
| 工作区 metadata filter | 不在范围 | 不实现 | v0.3.0+ |
| 工作区结构化查询增强 | 不在范围 | 不实现 | v0.3.0+ |

## 4. 风险与遗留

| 风险 | 等级 | 说明 | 缓解 |
| --- | --- | --- | --- |
| Linux 沙箱无 Wine, 无法跑 winwork.exe 真机验证 | 中 | "release 实跑" 在这台机只能是 headless + Tauri mock, 不算真运行时数据 | 用户侧 @kevinywb 在 Windows 真机回归 |
| Cargo.lock 仍未完全干净 (跨平台元数据噪音) | 低 | 不影响功能, 但每次 cargo build 都会重新触一次 | v0.2.30 顺手收掉 |
| Windows SmartScreen 拦首次下载 | 低 | 已知非代码问题, 用户体感差 | release note 写清 |
| wind-cli GitHub repo 路径可能改名 | 低 | trigger_install 走 fixed URL, 改名就 404 | 监控 GitHub releases |

## 5. 变更摘要 (vs v0.2.28)

### 新增 invoke (7 个 must-fix)
- `check_llm_wiki` (L0/L1 env status)
- `trigger_install` (L1 一键安装入口)
- `list_workspace` (L2 文件树)
- `list_wiki` (L2 文件树)
- `write_file` (L2 写文件 primitive)
- `write_workspace_artifact` (L2 orchestrator 写产物)
- `ai_chat` (L2 对话主路径)

### 修复 bug
- `index.html:1240` stale `switchTab` 重复声明 (B-scheme redesign 漏删, 整个 inline script 块静默失败)
- `js/lib/marked.min.js` 已 vendored 但 index.html 未加载, 这次真正生效

### 改进 UI
- L2 markdown 预览 (.md / .markdown 走 marked.parse)
- L2 文件名模糊搜索 (纯前端, debounce 120ms)
- L1 install modal 接入真实 trigger_install (不再 throw "command not implemented")
- 关键词路由 (mkdir/wft/run_wind_command) 加 try/catch
- wiki 关键词 (wiki_query/wiki_ingest/wiki_lint) 显式 amber card 指向 v0.3.0

### 性能 / 体积
- winwork.exe: 25,018,985 bytes (v0.2.28: 22,144,149 bytes) — A=7 handler 实现 + ai_chat HTTP 客户端
- zip: 16,376,171 bytes (v0.2.28: 14,213,088 bytes) — 多 2.2MB 主要是 winwork_lib.dll 增长
- sha256 zip: `fdd8741d524af96b3494401971a7198d0556ad6bf81854ed0f92b352688262b6`

### 自动化 gate (新增)
- `scripts/audit-invoke.sh` — 前端 invoke vs 后端 handler 差集
- `scripts/check-inline-scripts.sh` — `node --check` 每个 inline `<script>` 块

## 6. 签字

- 工程侧: @coder (本报告)
  - 所有"工程侧"结论基于 `origin/master` @ commit `6f79686911aecef3164bce7e8b3de551cbdd203c`
  - Release: https://github.com/wbyanclaw/winwork/releases/tag/v0.2.29
- 用户侧: @kevinywb / @yan — Pending (Windows 实机回归后填)
- 验收: @ruler — Pending (用户侧填齐后判 done)

## 7. 附录 — 工程侧 gate 输出

### 7.1 `cargo check`
```
   Compiling winwork v0.2.29 (/home/ubuntu/winwork/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.81s
```

### 7.2 `scripts/check-inline-scripts.sh`
```
5
  ✅ block 1 (1805 bytes)
  ✅ block 2 (1138 bytes)
  ✅ block 3 (1382 bytes)
  ✅ block 4 (366 bytes)
  ✅ block 5 (64471 bytes)

STATUS: OK — all 5 inline <script> block(s) pass node --check.
```

### 7.3 `scripts/audit-invoke.sh`
```
=== invoke audit ===
Frontend invoke() calls:    32
Backend registered handlers: 34

MISSING handlers (frontend calls without backend registration):
  - init_demo_workspace   (B group, try/catch in initWorkspace)
  - mkdir_dir             (B group, try/catch in keyword router)
  - run_wind_command      (B group, try/catch in keyword router)
  - wft_open              (B group, try/catch in keyword router)

STATUS: FAIL — 4 known B-group, all wrapped in try/catch with friendly
                user-facing fallback. Per Knife 1.5 classification.
```

### 7.4 `npm test`
```
ℹ tests 14
ℹ suites 0
ℹ pass 14
ℹ fail 0
```

### 7.5 文件 sha256 (release/winwork_release/)
```
c641d39245bc0868b26afb8fb1af52d8fa09d9af21d490d1b07b2543ff349ffa  winwork.exe
a0d20d14520f0bb134ae9b7a262b1d918260867e66f52b29f7454703774e4968  winwork_lib.dll
8427b1fc58ec707813e5c0a51eb5d69397bb333250a7b891be4d3b123f1e0f1c  WebView2Loader.dll
54c7d3942df7f8814f41c301f52800be54ee0e4285991f232a742e473c032c70  index.html
4ce1be2f55fdab74f52c7858d3426a9298065f3b0f9d11c0e3032e61569fbe62  styles.css
3e7e7d7feb3e5d58cb6c804f68ab5c24cc7e5eb6270fd6e5cbb9124739217d0c  js/lib/marked.min.js

zip: fdd8741d524af96b3494401971a7198d0556ad6bf81854ed0f92b352688262b6  winwork_0.2.29_x64.zip
```
